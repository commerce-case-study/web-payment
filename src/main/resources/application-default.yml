# Server
server:
  port: 7003

# Eureka Discovery Client Consumer
eureka:
  client:
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://localhost:8103/eureka

#redis
spring:
  cache:
    type: redis
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379